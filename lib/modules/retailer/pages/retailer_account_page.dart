import 'package:flutter/material.dart';

class RetailerAccountPage extends StatelessWidget {
  const RetailerAccountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Retailer Account Page Content'),
    );
  }
}