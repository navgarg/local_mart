import 'package:flutter/material.dart';

class RetailerAlertsPage extends StatelessWidget {
  const RetailerAlertsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Retailer Alerts Page'),
    );
  }
}