import 'package:flutter/material.dart';

class RetailerOrderHistoryPage extends StatelessWidget {
  const RetailerOrderHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Retailer Order History Page'),
    );
  }
}