import 'package:flutter/material.dart';

class WholesalerAlertsPage extends StatelessWidget {
  const WholesalerAlertsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Wholesaler Alerts Page'),
    );
  }
}